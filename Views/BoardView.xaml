<UserControl x:Class="Apex.Views.BoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#181825"
             PreviewKeyDown="BoardView_PreviewKeyDown">

    <Grid>
        <ScrollViewer x:Name="BoardScroll"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      Background="#181825"
                      PreviewMouseWheel="BoardScroll_PreviewMouseWheel">

            <Grid x:Name="CanvasContainer"
                  RenderTransformOrigin="0,0"
                  Width="10000"
                  Height="10000"
                  MouseLeftButtonDown="CanvasContainer_MouseLeftButtonDown"
                  MouseMove="CanvasContainer_MouseMove"
                  MouseLeftButtonUp="CanvasContainer_MouseLeftButtonUp"
                  ContextMenuOpening="CanvasContainer_ContextMenuOpening"
                  Focusable="True">

                <Grid.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform x:Name="ZoomTransform" ScaleX="1" ScaleY="1" />
                        <TranslateTransform x:Name="PanTransform" X="0" Y="0" />
                    </TransformGroup>
                </Grid.RenderTransform>

                <Canvas x:Name="BoardCanvas" Width="10000" Height="10000">
                    <!-- Cards are added dynamically here -->
                </Canvas>
            </Grid>
        </ScrollViewer>

        <!-- Reset view button overlay (top-right) -->
        <Button x:Name="ResetViewButton"
                Content="&#x2302; Reset view"
                FontSize="11"
                FontWeight="SemiBold"
                Padding="6,3"
                Cursor="Hand"
                Background="#313244"
                Foreground="#A6ADC8"
                BorderThickness="1"
                BorderBrush="#45475A"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="0,6,6,0"
                ToolTip="Reset view (Ctrl+Home)"
                Click="ResetViewButton_Click" />
    </Grid>
</UserControl>